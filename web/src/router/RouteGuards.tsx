import{useEffect}from"react";import{Navigate}from"react-router-dom";import{useAuthStore}from"@/store/authStore";import{useTenantStore}from"@/store/tenantStore";
// 恢复会话时一并填充租户 store，否则直接刷新 /tenant/* 页面会因为没有 activeTenant 而永远空白。
export function ProtectedRoute({children}:{children:React.ReactNode}){const{isAuthenticated,loading,loadSession}=useAuthStore();useEffect(()=>{if(loading)void loadSession().then(auth=>{if(auth)useTenantStore.getState().hydrate(auth)})},[loading,loadSession]);if(loading)return <div className="p-12 text-center">Loading...</div>;return isAuthenticated?<>{children}</>:<Navigate to="/login" replace/>}
export function PublicRoute({children}:{children:React.ReactNode}){const{isAuthenticated,loading,loadSession}=useAuthStore();useEffect(()=>{if(loading)void loadSession()},[loading,loadSession]);if(loading)return <div className="p-12 text-center">Loading...</div>;return isAuthenticated?<Navigate to="/dashboard" replace/>:<>{children}</>}
