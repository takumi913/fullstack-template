import{create}from"zustand";import{tenantApi,type Tenant,type TenantMember}from"@/api";
interface TenantState{tenants:Tenant[];activeTenant:Tenant|null;membership:TenantMember|null;members:TenantMember[];loadTenants:()=>Promise<void>;selectTenant:(tenant:Tenant)=>Promise<void>;loadMembers:()=>Promise<void>;reset:()=>void}
export const useTenantStore=create<TenantState>((set,get)=>({tenants:[],activeTenant:null,membership:null,members:[],loadTenants:async()=>{const r=await tenantApi.list();const current=get().activeTenant;set({tenants:r.data,activeTenant:current&&r.data.some(t=>t.id===current.id)?current:r.data[0]??null})},selectTenant:async(tenant)=>{await tenantApi.select(tenant.id);set({activeTenant:tenant,membership:null,members:[]});await get().loadMembers()},loadMembers:async()=>{const t=get().activeTenant;if(!t)return;const r=await tenantApi.members(t.id);set({members:r.data,membership:r.data.find(m=>m.user_id===useAuthStore.getState().user?.id)??null})},reset:()=>set({tenants:[],activeTenant:null,membership:null,members:[]})}));
import{useAuthStore}from"./authStore";
